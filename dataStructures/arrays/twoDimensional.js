// Each row is represented by a separate list within the outer list
const matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
];

// To access individual elements of the matrix, you use the row and column indices like this:
const element = matrix[1][2] 
console.log(element); // 6